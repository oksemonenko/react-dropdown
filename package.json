{
  "name": "react-dropdown",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://oksemonenko.github.io/react-dropdown",
  "dependencies": {
    "custom-react-scripts": "^0.2.2",
    "prop-types": "^15.6.2",
    "react": "^16.4.1",
    "react-dom": "^16.4.1",
    "yarn": "^1.9.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "devDependencies": {
    "gh-pages": "^1.2.0"
  },
  "eslintConfig": {
    "extends": "react-app"
  }
}
