{
  "name": "test-iq-option",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^16.4.1",
    "react-dom": "^16.4.1",
    "react-scripts": "1.1.4",
    "stylus-chokidar": "0.0.3"
  },
  "scripts": {
    "build-css": "stylus-chokidar src/ -o src/",
    "watch-css": "npm run build-css && stylus-chokidar src/ -o src/ --watch",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}